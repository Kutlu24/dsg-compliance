.env
__pycache__/
*.pyc
.pytest_cache/
data/raw/
*.egg-info/
# data/chroma/ is no longer committed: it now holds Gemini-embedding
# vectors (see embeddings.py) rebuilt automatically by Render's
# buildCommand on every deploy (see render.yaml), so a committed copy
# would just go stale and risk an embedding-dimension mismatch.
data/chroma/
