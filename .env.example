# Copy to .env and fill in real values. Never commit .env.

# Claude API key, for the RAG chat backend and (optionally) embeddings.
ANTHROPIC_API_KEY=

# Where the Chroma vector store persists its data.
CHROMA_PERSIST_DIR=./data/chroma
